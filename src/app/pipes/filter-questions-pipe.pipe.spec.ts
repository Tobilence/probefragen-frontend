import { FilterQuestionsPipePipe } from './filter-questions-pipe.pipe';

describe('FilterQuestionsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterQuestionsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
